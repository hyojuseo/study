import 'package:flutter/material.dart';

class ContainerEx extends StatelessWidget {
  const ContainerEx({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Container(width: 50, height: 50, color: Colors.blue));
  }
}
